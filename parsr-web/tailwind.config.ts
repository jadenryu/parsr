export default {
  darkMode: "class",
};
/* Previously:
import type { Config } from "tailwindcss";
import animate from "tailwindcss-animate";
export default { content: [...], theme: {...}, plugins: [animate] } satisfies Config;
*/
